import 'package:flutter/material.dart';

class EAntrian extends StatelessWidget {
  const EAntrian({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("EANTRIAN"),
      ),
    );
  }
}